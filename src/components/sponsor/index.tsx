import { title } from '../ui/text/title';

export default function Sponsor() {
  return (
    <section
      id='sponsors'
      className='min-h-screen grid place-items-center'
    >
      <title.h1>Sponsor</title.h1>
    </section>
  );
}
